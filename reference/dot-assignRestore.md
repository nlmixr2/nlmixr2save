# Return if the last `:=` assignment was actually a restore from a file

Return if the last `:=` assignment was actually a restore from a file

## Usage

``` r
.assignRestore()
```

## Value

`TRUE` if the last `:=` assignment was a restore from a file, `FALSE`
otherwise

## Author

Matthew L. Fidler
